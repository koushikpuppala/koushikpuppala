# Ensures dependencies are installed using pnpm's default linker
# (shared store, no hoisting, minimal disk usage)
node-linker=pnpm

# Define the version prefix for installed packages (use ~ for patch updates)
save-prefix=~

# Enable colors in the terminal output
color=true

# Always install peer dependencies automatically
auto-install-peers=true
